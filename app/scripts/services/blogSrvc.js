(function(module){
    'use strict';
    module.service('blogSrvc',['$q', '$http', 'appConfig', function ($q, $http, appConfig) {
        var PAGE_SIZE = 30;
        this.getPosts () {
             
        }
    }]);
}(angular.module('tenshinApp.services')));